# mock-data 目录说明

本目录存放可直接给 Codex 参考或复制到 `src/data/mock/` 的 JSON 示例数据。

注意：

1. 当前 mock 数据只用于 UI 演示；
2. 不代表真实采购价格；
3. 后续 Codex 可以扩展到 30-50 条；
4. 字段必须遵守 `field-dictionary/`；
5. 关系逻辑必须遵守 `mock-data-spec/11_MOCK_DATA_RELATIONSHIP_RULES.md`。
