NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_publishable_key
# Equipment review AI provider. P1 defaults to the deterministic adapter;
# replace the provider registry with a server-only model adapter before using a real API.
WPI_AI_REVIEW_PROVIDER=deterministic
