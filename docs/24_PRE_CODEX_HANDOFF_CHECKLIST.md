# 24 交给 Codex 前的准备清单

## 一、当前不要执行

- 不要让 Codex 直接写代码；
- 不要让 Codex 直接做 28 页；
- 不要让 Codex 先接数据库；
- 不要让 Codex 先接 AI API。

## 二、交给 Codex 前应确认

| 检查项 | 状态 |
|---|---|
| 28 页 UI 参考图是否在 `visual-references/ui-images/` | 已完成 |
| Image2 资产是否在 `assets/backgrounds/` 和 `assets/illustrations/` | 已完成 |
| SVG 图标是否在 `assets/svg-icons/` | 已完成 |
| Image2 / SVG / 代码边界是否明确 | 已完成 |
| 页面路由与资产关系是否明确 | 已完成 |
| 组件库计划是否已记录但暂缓 | 已完成 |

## 三、下一步如果继续补规范

建议补：

1. mock 数据清单；
2. 字段字典；
3. API 草案；
4. 页面验收表；
5. Codex 分轮提示词。
