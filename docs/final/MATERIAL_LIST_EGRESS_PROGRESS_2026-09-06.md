# 地材主列表流量治理进度

状态：字段裁剪已本地验证；全量分页和聚合未完成，R05仍开放。

## 调用核查

- `src/app/material-prices/page.tsx`通过GET `/api/material-prices`加载数据，随后在客户端筛选、分页、统计及分析；尚不能把审核队列的分页结论用于此页面。
- `MaterialImportCenter`与`MaterialPriceForm`对同路径使用POST，单条编辑读取独立详情接口。此次不改变详情或写入内容。
- 供应商API还被供应商主页、设备新增表单、询价信页面与核验hook使用，后续需按主列表、选项查询、审核索引分别治理，不能统一截断。

## 已改

- GET移除select星号和完整metadata读取。显式返回18个顶层字段、供应商id/legacy_id/name，以及15个被列表、映射和趋势分析使用的metadata键。
- JSON字段使用箭头投影保留数字和布尔类型，恢复原metadata响应结构；含税标记、来源线索编号、报价日期不丢失。
- 详情中的原始证据、PDF正文、文件编码等不进入主列表；没有删除数据库字段或业务记录。
- 传递请求取消信号，返回private/no-store；失败不显示伪造空列表，也不向客户端返回底层数据库错误详情。

## 验证

- `scripts/material-list-projection.test.mjs`：2项通过，裁剪后映射字段与趋势结果保持一致，合成大文本不进入列表。
- `scripts/material-review-api.test.cjs`与`material-review-queue.test.cjs`：18项通过，含真实GET路由投影、组织条件、认证拒绝和原有写入回归。认证及数据库传输由合成测试替代，不是云端数据库验收。
- 本轮TypeScript和相关文件ESLint通过。

## 剩余风险

- GET仍是未分页读取，受数据库API行数上限影响；不能宣称主列表总数或导出覆盖完整组织。需要服务端分页、独立统计/筛选选项、趋势接口和完整范围导出一起改造。
- 本轮不报告生产节流百分比或每月GB预测；缺少生产查询响应大小、请求频率及计费观察证据。
- 未部署云端、未应用迁移、未创建评审环境、未修改正式数据。
